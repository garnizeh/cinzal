# Decision log

A decision is a question that must be answered **in writing** before dependent work can start. Decisions produce a document, not code, and they **block the tasks that depend on them** — a milestone does not open with blockers outstanding.

The catalogue below comes from [roadmap §3](../project/cinzal-implementation-plan.md), which found these while planning the GDD and the RFC against each other. Most are not implementation details: they are places where the specifications are silent, ambiguous, or contradict themselves.

## Format

One file per decision, named `Dnn-short-slug.md`:

```markdown
# Dnn — <the question, as a question>

**Status:** open | decided | superseded by Dnn
**Blocks:** M0 | M1 | …
**Decided:** YYYY-MM-DD

## The question
## Why it is open
   What the specs say, and where they disagree. Cite sections.
## Options
   At least two, each with what it costs.
## Decision
## Reasoning
## Consequences
   What changes downstream, and what it would cost to reverse later.
```

Two conventions worth keeping:

**Record the rejected options and why.** The reasoning outlives the verdict — when a decision comes back around, the useful artefact is the argument, not the answer. Both specs are written this way and it is the single most valuable property they have.

**A decision that turns out wrong is superseded, not edited.** Leave the original standing with a pointer forward.

## Catalogue

Status is `open` until a document exists here.

### Blocks M0 — Foundations

| # | Question | Status |
|---|---|---|
| [D1](D01-package-layout.md) | Where does `MatchState` live relative to `PlayerView`? RFC §5 and §3 contradict each other, and the CI check cannot be written until this is settled | **decided** — leaf `internal/game`; `render` and `web` may not directly import `internal/rules` |
| [D2](D02-order-draft-state.md) | Where does the order draft live between clicks? | **decided** — stateless, carried in the form |

### Blocks M1 — Rules core

| # | Question | Status |
|---|---|---|
| [D3](D03-rng-consumption-table.md) | The RNG consumption table is incomplete — at least eight consumers implied by the card text are missing from RFC §6.4 | **decided** — all ten gaps closed with a mandated method each, including Riot's, completed by [D4](D04-riot-trail-randomization.md) |
| [D4](D04-riot-trail-randomization.md) | `Riot` has no specification, and it has a fog dimension | **decided** — permutes the flagged sector's current-round sight-gated trail entries among themselves; global entries untouched, no name is ever invented, no new row lands in RFC §9.1 |
| [D5](D05-upkeep-phase.md) | Phase 8 (Upkeep) is never enumerated in either document | **decided** — five-step dependency-ordered sequence (flags clear, contracts, leases, Sinkhole, next-round modifiers); Contact Cooldown and crate heat turn out to belong elsewhere, not in Upkeep |
| [D6](D06-contract-tier-mix.md) | The contract offer has no tier distribution | **decided** — one slot targets the highest eligible tier (subject to D7's fallback if that pool is empty); the other two are a weighted draw over eligible tiers, default uniform |
| [D7](D07-contract-pool-fallback.md) | Contract generation can produce an empty or short pool mid-match | **decided** — drop to a lower tier, then offer fewer than three; hold the offer (cooldown not restarted) when the whole pool is empty |
| [D8](D08-sector-size-constraint.md) | The sector size constraint is arithmetically impossible at 15, 16 and 12 nodes | **decided** — per-sector minimum lowered from 4 to 3 nodes; sector count stays four everywhere |
| [D9](D09-node-type-rounding.md) | Node type shares do not divide at 15, 22 and 28 nodes, and no rounding rule is stated | **decided** — largest remainder, ties broken by §6.2's declaration order |
| [D10](D10-map-layout.md) | Map generation produces no 2D layout, which the projection and the SVG both need | **decided** — coordinates generated in `rules/gen` on a fixed 1000×1000 canvas; four sectors sit in fixed quadrants, nodes placed by partial Fisher-Yates over a per-quadrant 9-point lattice |
| [D11](D11-config-suppression-flags.md) | `Config` has no subsystem-suppression flags, which solo scenarios require | **decided** — one boolean per subsystem in a nested `SubsystemSuppression` struct (Leases, Incidents, Events, InfamyTiers, Items); no separate Pressure flag, since it's unreachable once InfamyTiers is suppressed |
| [D12](D12-decoy-fog-writer.md) | `Decoy` is unspecified at the fog boundary | **decided** — plants a real "Cargo Taken" entry naming only the planter (self-misdirection, never another player), gated by the planter's own Infamy ≥ 3 at end-of-round resolution — row 1's existing gate, not a new one; row 12 in RFC §9.1's table |
| [D13](D13-observation-denominator.md) | `Blackout` and `Rain` distort the observation archive's denominator | **decided** — excluded only where a real entry was actually erased, not per whole round; `SeatArchive` gains a parallel `Obscured` set; Vanish, Distracted Guard and Festival need no special case, since they suppress the actor's own entry by design, not the world's |
| [D14](D14-five-resolution-gaps.md) | Small resolution gaps: `Torched` at zero lease, `Muscle` in a melee, buying at the hand limit, `Open Doors` stock, `Bounty` tie-break | **decided** — one authoritative Torched expiry via Upkeep's own check (widened to ≤0); melee losers lose Muscle, ties lose nobody's; a new `Legal` row rejects Deal at hand limit unless a same-order discard frees it; `Open Doors` draws from a Black Market already in the player's sight, declared up front; `Bounty` is a single target tied by the fairness key, matching `New Boss` |
| ~~D15~~ | Two cross-reference errors in the source documents: the non-existent "Blitz" card, and a stale post cap of 5 | **not a decision** — reclassified as a task, [#40](https://github.com/garnizeh/cinzal/issues/40) |

**On D15.** It arrived in [roadmap §3.2](../project/cinzal-implementation-plan.md) alongside twelve genuine decisions and it is not one of them. RFC §6.5 cites a card called "Blitz" that GDD §14.2 does not contain — the card with that behaviour is **Raid** — and GDD §9.2 still caps Stake Post at 5, which §10.3 replaced with 4/4/4/3. There is no question to answer and no option to weigh: both are wrong sentences, so they produce a pull request against the specs rather than a document here.

The row stays, struck through, because the numbering is cited elsewhere and a silently vanished D15 reads as an oversight. **D3–D14 are the twelve decisions that block M1** — joined since by D23–D30, surfaced later in M1's own implementation rather than in the original roadmap pass. D31 surfaced the same way but blocks nothing in M1 itself — its own dependents are a fog test asserting on `NodeView.Name` and M5's map rendering; see the table below for the complete current list.

| # | Question | Status |
|---|---|---|
| [D23](D23-starting-fog-seeding.md) | GDD §6.1 constraint 7 says starting nodes "begin Known" along with a nearby Warehouse, but no document states which nodes exactly — surfaced while scoping [#61](https://github.com/garnizeh/cinzal/issues/61) | **decided** — two composed mechanisms: §7.2's ordinary sight rule applied at round 0 (starting node + neighbours), plus constraint 7 seeding every still-Hidden Warehouse within 2 steps as Known (never downgrading one already in sight); no tie-break needed either way |
| [D24](D24-opening-offer-guarantee.md) | D7 claims reachability alone guarantees a non-empty opening contract offer, but that assumes a 3-step Warehouse/Border floor GDD §6.1 constraint 6 does not actually provide (only 2, non-adjacency), and a Nobody's eligible tier set (Tier I alone) doesn't span D7's own "bands cover everything ≥3" argument — surfaced while implementing [#63](https://github.com/garnizeh/cinzal/issues/63), confirmed with a concrete counterexample | **decided** — the fix belongs in constraint 7, not the contract rules: a start position now needs a Warehouse within 2 steps that itself has a Border 3–4 steps away (Tier I's band), so the pair the opening offer needs provably exists; 7.10%→0.00% of two-player seats over 1000 seeds, D6/D7/D23 unamended, no RNG change |
| [D25](D25-item-market-resolution-gaps.md) | Four item/market gaps surfaced while scoping [#66](https://github.com/garnizeh/cinzal/issues/66): which rounds a Black Market's stock refreshes on, what Bolt Hole's "2 steps away" is measured from, whether Police Band can target any node, and whether market stock can repeat an item | **decided** — refreshes odd rounds (1,3,…,15), not the "even rounds" #66 assumed; Bolt Hole measures from the player's start-of-round position; Police Band can target any node the player isn't Hidden to (Rumoured or better); market stock draws 3 distinct items via partial Fisher-Yates, no duplicates |
| [D26](D26-sector-incident-fog-writer-rows.md) | `EventInformantRing` and `EventSpilledLoadCrate` (#73's sector incidents) fit the §9.1 position-writer shape but have no table row — surfaced while `internal/rules/anchors.go` provisionally folded both into rows 11 and 13 pending this decision | **decided** — two new rows, 15 (Informant Ring) and 16 (Spilled Load), citing GDD §14.3 directly; a row is defined by its named GDD source, not by matching disclosure columns, matching the precedent rows 5/6, 9/10, 12 and 13/14 already set; no functional code change, `buildRoundAnchors` already grouped both correctly by shape |
| [D27](D27-project-config-parameter.md) | `Project`'s decided signature has no `Config`, but `StepAllowance`/`RoundsToNextOffer`'s own field docs say `Project` computes them from one — surfaced while scoping [#75](https://github.com/garnizeh/cinzal/issues/75) | **decided** — `Project`'s signature stands unamended; those two fields are `internal/match`'s responsibility to fill in from `rules.Steps`/`rules.RoundsToNextOffer` once `Project` returns |
| [D28](D28-dragnet-rotating-borders-fallback.md) | GDD §6.3's rotating borders and §14.2's Dragnet were specified independently; at 2 players (3 Borders total) the two can combine to seal every Border at once, leaving Dragnet's own "route to the ones that remain" with nothing to route to — surfaced while scoping [#76](https://github.com/garnizeh/cinzal/issues/76) | **decided** — neither mechanic changes; a downstream safety-valve check reopens the lowest-`NodeID` Border whenever Dragnet's seal and rotation's inactive set would together cover the whole map; structurally inert at 3+ players, where it can never trigger |
| [D29](D29-phase-2-3-fold-attachment.md) | GDD §15's Phases 2 (contract offer) and 3 (market refresh) are exported functions with no caller anywhere outside a test, absent from `Resolve` and from RFC §6.7's pipeline diagram, and the contract accept/decline choice has no field on `Order` to travel through the fold — surfaced while scoping [#152](https://github.com/garnizeh/cinzal/issues/152) | **decided** — both phases draw a round ahead, at the tail of `Resolve` (round 1 bootstrapped once, unconditionally, inside `initial`), the identical timing `UnstableSector` already uses; the contract choice becomes `Order.ContractChoice *int`, applied at the head of the following `Resolve` call |
| [D30](D30-contended-action-loss-notification.md) | Four action categories in `internal/rules` — Deal, Pickup, Stake Post's cap, the Ledger — fail with no event at all when they can no longer complete by Step N+1/N+3, and Open Doors fails the same way; all five cite D14's Open Doors ruling as precedent, but D14's reasoning was about a pre-declared boon that may never fire, not a declared action that loses a fair race or finds its own state no longer sufficient — surfaced while scoping [#153](https://github.com/garnizeh/cinzal/issues/153) | **decided** — GDD §15.0's "never silently fail" isn't Step-0-scoped; it applies at every resolution step. Six new private, actor-scoped `EventKind` values (one per distinct cause across the four categories) close the gap, exempt from RFC §9.1's writer table for the same reason the existing Step 0 kinds are; Open Doors is unaffected, D14 §4 still governs it |
| [D31](D31-node-display-name.md) | `Node.Name` and `NodeView.Name` both exist and are both disclosed from Rumoured onward (GDD §7.1), but nothing ever assigns either one — surfaced while scoping [#154](https://github.com/garnizeh/cinzal/issues/154) | **decided** — deterministic, RNG-free: sector + type + 1-based rank among same-type nodes in that sector (e.g. "Old Docks Warehouse 2"), assigned once at map generation; no new `Purpose`, no drawn name pool |

### Blocks M2 — Bots and simulation

| # | Question | Status |
|---|---|---|
| [D32](D32-bot-rng-stream.md) | `Decide(v, cfg, r *rules.RNG)` takes the seeded RNG, but RFC §6.4's consumption table has no row for it, and Autopilot handover means a shared stream would make `Resolve`'s own per-round index count depend on when a human stopped submitting — surfaced while scoping [#186](https://github.com/garnizeh/cinzal/issues/186) | **decided** — bot draws never touch `Resolve`'s `*RNG`; a new, distinct `BotRNG` type, constructed by `NewBotRNG(matchSeed, seat, round)`, gives each bot seat its own stream per round, deterministic in `(seed, seat)` and independent of any other round's human/bot history — `BotRNG` has no `Next` method and `RNG` has no `NextBot`, so the two streams can't be mixed up at a call site; `BotRNG`'s own open `BotPurpose`/`NextBot` sits beside the closed `Purpose`/`Next`, since a bot's draw count is not a §6.4-auditable constant; bot orders are still logged and a refold never calls `Decide`, so `cmd/simulate`'s reproducibility and #81's index-count guarantee both hold unconditionally |
| [D33](D33-telemetry-event-stream-coverage.md) | Which GDD §22 metrics are computable from the Event stream, and what does the rest need? — surfaced while scoping [#187](https://github.com/garnizeh/cinzal/issues/187) | **decided** — of GDD §22's twenty per-match rows: five need nothing new (2, 10, 11, 17, 20, one grouping caveat); six need a final `MatchState` read, two of them (sight share, Heat Map confidence) fog-private per-seat archives no `Project()` call ever aggregates (3, 4, 5, 7, 8, 19); four need small, targeted additions to `internal/rules` — one new event each for confrontation-caused route halting, a silent-incident-resolver hit signal, and sector-incident exposure, plus one new `Stance` field on `Event` (rows 1, 6, 9, 12); row 1's denominator needs order-log access regardless; rows 13/14 (card-attributed RP swing and confrontations) are the one genuinely expensive pair, scoped down rather than answered exactly; row 16 defers to M5 UI instrumentation, rows 15 and 18 to M5.5 — the second because no operational definition exists to compute it against |
| [D34](D34-telemetry-package-placement.md) | Where does the telemetry computation live, and what is its input type? — surfaced while scoping [#188](https://github.com/garnizeh/cinzal/issues/188), paired with D33 | **decided** — `internal/telemetry`, imports `internal/rules`, sitting beside `bots` in RFC §5's layout; imported by `cmd/simulate`, `internal/match`'s M4 analytics path, and the debug panel, never `render`/`web` — `scripts/check-fog-boundary.sh`'s `FORBIDDEN` set gains `internal/telemetry` alongside `internal/rules`, landing in #197's PR, matching D01's own blanket-prohibition precedent rather than reasoning about what `Match`'s signature happens to require; `Match(s rules.MatchState, log rules.OrderLog, events []game.Event, cfg game.Config) (MatchSummary, error)`, with a new `OrderLog` type placed in `internal/rules` rather than `internal/game`, on the same reasoning D01 kept `MatchState` out of `game` — a full-match order log names every seat's history, not one seat's, and is at least as fog-sensitive |
| [D35](D35-simulation-sample-size-and-verdict-rule.md) | How many matches per configuration, and what makes a threshold verdict actionable? — surfaced while scoping [#189](https://github.com/garnizeh/cinzal/issues/189) | **decided** — 10,000 matches per configuration (matching RFC §16.4's own worked example, already load-bearing in [#200](https://github.com/garnizeh/cinzal/issues/200)/[#204](https://github.com/garnizeh/cinzal/issues/204), not GDD §20's 3,000-match R2 precedent); every metric reduced to one number per match first, then `mean ± 1.96·s/√n`, one formula for rates, counts and 0/1 indicators alike; action only when the interval clears the threshold, a straddling baseline check gets one second-root-seed re-run, pooled with the first into one 20,000-match interval, before being parked as "watch, unresolved"; no cross-metric multiplicity correction, since the seven thresholds are independent design questions, not one hypothesis family; R9 and the two-player floor read against Drifter, R1/R6/R7/R11/the lease rate against Operator, both tiers always reported |
| [D36](D36-lease-rate-chokepoint-gate.md) | The lease-rate exit criterion can't be met by a `Config` edit — loosen `OperatorOptions`' chokepoint gate, or recalibrate GDD §22's 2–4/player band? — surfaced by [#204](https://github.com/garnizeh/cinzal/issues/204)'s own root-cause finding, filed as [#231](https://github.com/garnizeh/cinzal/issues/231) | **decided** — neither: a D35-rigor paired-cohort sweep against the gate, at 4 players (10,000 matches, 95% intervals), shipped default vs. fully open, shows the lease rate interval tops out at [0.279, 0.289]/player — still 7–14× below the band, and below even the `<1` failing floor — at a paired RP-margin interval [−0.242, −0.201] entirely on the losing side of zero, closing the loosening option at both tested endpoints and undercutting the recalibration option's premise that the gate alone explains Operator's low leasing; deferred to M5.5's human playtesting per the roadmap's own "a sweep narrows the range, M5.5 confirms it" caveat, with Operator's contract-first priority ordering — a candidate next constraint, not established here — filed separately as [#237](https://github.com/garnizeh/cinzal/issues/237), blocked on that human data |
| [D37](D37-five-player-confrontation-load.md) | The 5-player R9 threshold can't be met by node count alone — raise D8's per-sector ceiling, add a fifth sector, or accept the row unmet? — surfaced by [#229](https://github.com/garnizeh/cinzal/issues/229)'s own ceiling analysis, filed as [#232](https://github.com/garnizeh/cinzal/issues/232) | **decided** — accept the row unmet and defer to M5.5, after measuring the option `#232` could not: a D35-rigor sweep from 28 to 52 nodes at 5 players (Drifter, 10,000 matches per configuration, two root seeds) shows R9's `> 12` threshold *does* clear by node count, at **46 nodes**, crossing between 44 (12.08 / 12.15) and 46 (11.59 / 11.62) — but by then §22's share-of-map-under-sight row has left its 30–55% band (0.281) and its Heat-Map-low-confidence row has left its `< 40%` target (0.445) — the two nearest **proxies for** the "Board goes unused" leading indicator R9's own text (§20) names as the thing to watch, which has no computed row of its own ([#233](https://github.com/garnizeh/cinzal/issues/233)) — so R9 clears only past the point its own indicator has tripped; reaching 46 would also reopen D10's layout lattice (9 → 16 cells, minimum separation 175/150 → 130/110) and not only D8, and a fifth sector (option B) has no quadrant to be drawn in under D10's four-way canvas partition; the sweep also found `#232`'s and `#229`'s stated 32-node generator ceiling is wrong — nothing in production code enforces D8's `[3, 8]` range, the real ceiling is 36, and 37+ panics rather than failing validation ([#239](https://github.com/garnizeh/cinzal/issues/239)) |
| [D38](D38-board-going-unused-indicator.md) | R9's "Board going unused" leading indicator has no computed row — define one as map utilization, ship it as a diagnostic, or leave it unimplemented? — surfaced by [#229](https://github.com/garnizeh/cinzal/issues/229)'s own "check for an existing row first" bullet, filed as [#233](https://github.com/garnizeh/cinzal/issues/233) | **decided** — none of the three: the pointer already names something and was only missing its row numbers. "The Board" is §7.5's **deduction UI**, capitalised as a proper noun in both places the phrase appears, and §22 already measures its use in rows **15** (attribution queries) and **16** (Heat Map opens) — the same two the roadmap's P6 risk calls *"the check that it is being used"* — both UI instrumentation, deferred by [D33](D33-telemetry-event-stream-coverage.md) to M5.5/M5 and **structurally unavailable to bot simulation**, since a bot opens no Heat Map; so R9's indicator was never M2's to produce, and D37 could not have had it whatever `#233` built; the headless guardrail on R9's *remedy* is the other pair, rows **8** and **19** — a map raised far enough leaves the Board's data too thin to deduce from, which is exactly what D37 measured and mislabelled as a proxy; option A is rejected on measurement as well as naming — at D35 rigor (10,000 matches, two root seeds, Drifter) "share of unused nodes" falls monotonically as the map shrinks, reading healthiest exactly when R9 is worst, and ranks the accepted 4-player map as more unused than the 5-player map that trips R9; no new row, field, event or CSV column, and every §22 band is unchanged |
| [D39](D39-r1-confrontation-softening.md) | R1 tripped at every player count and §20's remedy is hedged — what exactly gets softened, against a numerator nobody had split? — surfaced by [#205](https://github.com/garnizeh/cinzal/issues/205)'s own exit demonstration, filed as [#245](https://github.com/garnizeh/cinzal/issues/245) | **decided** — two parts, neither of which works alone. **§22 row 1's numerator is re-specified to count routes**, one per (round, seat) whose submitted route was cut before its last step, not one per `EventRouteHalted` — as instrumented it counted halt *events* against a denominator of submitted *routes*, so roughly half of it was catches on seats that submitted no route, second catches on a route already gone, or catches on a plan that had already run out; D33 had refused raw event counting for row 10 on this exact reasoning and never applied it to row 1. The threshold, band, denominator and verdict tier are unchanged, and under the corrected count the shipped rule **still fails at every player count** (17.3%–29.1%, Operator), which is what makes the correction safe to make alongside the remedy rather than instead of it. And **every confrontation participant whose declared route can no longer be walked keeps the round's remaining *steps***, spending them as §9.1 blind steps from wherever they now stand — with the confrontation node excluded from the first of them for a loser or a tie participant, who are somewhere else and could walk back in, and no exclusion for a crossing-corrected winner, who is standing on it — while the declared route and the action stay forfeit. That is one rule for all three halt sites, and two of them were never rules: §15 never says a **tie** forfeits a round (`resolveTie` called its own halt "an implementation necessity"), and it says outright that the **winner** *"holds the node; their route continues"* — yet a crossing-corrected winner is halted anyway. §20's own guess — *"letting the loser continue their route from the fallback node"* — is rejected **on measurement**: it cannot be executed without retracing through the node the loser was just pushed out of, the C → D → C shape §15's pushback clause calls *"worse than no rule"*, which happens to ~90% of continued losers; it clears the corrected line at 5 players by only 0.79 pp; and the tie extension that would give it margin costs §22 row 9 +5.5 pp toward its own failing line, while the adopted rule leaves row 9 where it found it and improves rows 2, 3 and 17. Half-steps (`#245`'s "applied partially") is closed outright — 16.32% at 4 players and 19.20% at 5, both intervals above the line, and straddling it at 3 |
| [D41](D41-operator-runner-margin-post-d39.md) | D39 softened what a confrontation loss costs, and `TestOperatorBeatsRunnerOverAThousandMatches` (#194's own M2 acceptance criterion) now fails on its committed golden seed with the margin's sign unstable across other roots — surfaced by [#259](https://github.com/garnizeh/cinzal/issues/259), filed as [#265](https://github.com/garnizeh/cinzal/issues/265) | **decided** — a D35-rigor paired-margin measurement (10,000 matches per root, two independent roots, pooled to 20,000 on the first pair straddling zero) finds Operator's RP margin over Runner at +0.0018 [−0.0074, +0.0111] — statistically indistinguishable from zero, D35's own "watch, unresolved at n = 20,000" terminal state, handed to M5.5 rather than inflated further; the test's `margin <= 0` failure is removed (report-only, keeping its two fail-closed structural checks and margin logging) rather than swapped to a favorable seed, since the pooled measurement shows the true margin was never far from zero — D39 moved a near-coin-flip, it didn't break a solid signal |

### Blocks M5 — Playable web, and M6 — Async

| # | Question | Status |
|---|---|---|
| D16 | The Recap has no per-seat cursor | open |
| D17 | Invite links have no storage | open |
| D18 | Pins and notes are promised in v1 scope and have no storage | open |
| D19 | Per-match email preferences have no storage | open |
| D20 | Rate-limit state has no home, and in-process counters are wrong across two instances | open |
| D21 | i18n is in scope and has no design | open |
| D22 | Match abandonment is undefined, so `matches.status = 'abandoned'` is unreachable | open |

## Also open, from RFC §20

Carried from the RFC's own list, with the point at which each needs an answer:

| # | Question | Needed by |
|---|---|---|
| Q1 | TinyGo for the WASM binary | RFC-002 |
| Q2 | What the map needs once RFC-002 adds interaction | after M5.5, deliberately |
| Q3 | One-click resubmit from an email link | M6 or v1.1 |
| Q4 | Filler bots in ranked play | post-v1 |
| Q5 | Multi-region | not needed; recorded so nobody adds it speculatively |
| Q6 | Guest session loss disclosure on the join page | M5 |
